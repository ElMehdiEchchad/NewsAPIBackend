module.exports = {
    host: "localhost",
    user: "root",
    password: "mehdi123",
    DB: "api_news"
    
  };

  //if the mysql server returns client does not support authentification 
  //protocol .... 
  //apply these changes to your mysql user; 
  //ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'New_Password'
  //flush privileges;
